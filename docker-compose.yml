version: '3.9'

services:
  api:
    build: .
    container_name: DBWebApi
    ports:
      - "${API_PORT}:8000"
    env_file:
      - .env
    volumes:
     - ./data:/app/data
    restart: unless-stopped